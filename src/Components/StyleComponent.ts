export class StyleComponent {
  fill: string;
  width: number;
  height: number;

  constructor(fill: string, width: number, height: number) {
    this.fill = fill;
    this.width = width;
    this.height = height;
  }
}
